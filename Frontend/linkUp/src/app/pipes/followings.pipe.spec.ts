import { FollowingsPipe } from './followings.pipe';

describe('FollowingsPipe', () => {
  it('create an instance', () => {
    const pipe = new FollowingsPipe();
    expect(pipe).toBeTruthy();
  });
});
